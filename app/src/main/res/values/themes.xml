<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OnlineCinema" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="activeIndicatorStyle" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/transparent</item>
    </style>

    <style name="searchViewStyle">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:queryHint">@string/searchHintText</item>
        <item name="hintTextColor">@color/message_color</item>
        <item name="searchHintIcon">@null</item>
    </style>

    <style name="spinner" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:textColor">@color/primary_color</item>
        <item name="android:textSize">20sp</item>
        <item name="android:backgroundTint">@color/background</item>
    </style>

    <style name="Theme.OnlineCinema" parent="Base.Theme.OnlineCinema" />
</resources>